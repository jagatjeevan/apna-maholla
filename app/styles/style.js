import { StyleSheet } from 'react-native';
import styleConstants from './styleConstants';

const styles = StyleSheet.create({
  appContainer: {
    alignItems: 'center',
    backgroundColor: styleConstants.body.backgroundColor,
    flex: 1,
    justifyContent: 'center'
  },
});

export default styles;
